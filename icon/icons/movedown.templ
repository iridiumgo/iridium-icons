package icons

import "github.com/iridiumgo/iridium-icons/icon"

var MoveDown = icon.NewIcon(MoveDownComponent)

templ MoveDownComponent(i *icon.Icon) {
	<svg
		xmlns="http://www.w3.org/2000/svg"
		width={ i.Width }
		height={ i.Height }
		viewBox={ i.ViewBox }
		fill={ i.Fill }
		stroke={ i.StrokeStr }
		stroke-width={ i.StrokeWidthStr }
		stroke-linecap={ i.StrokeLineCap }
		stroke-linejoin={ i.StrokeLineJoin }
		{ i.AttributesMap... }
	>
		<path d="M8 18L12 22L16 18"></path>
		<path d="M12 2V22"></path>
	</svg>
}
