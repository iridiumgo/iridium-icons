package icons

import "github.com/iridiumgo/iridium/view/icon"

var Clock5 = icon.NewIcon(Clock5Component)

templ Clock5Component(i *icon.Icon) {
	<svg
		xmlns="http://www.w3.org/2000/svg"
		width={ i.Width }
		height={ i.Height }
		viewBox={ i.ViewBox }
		fill={ i.Fill }
		stroke={ i.StrokeStr }
		stroke-width={ i.StrokeWidthStr }
		stroke-linecap={ i.StrokeLineCap }
		stroke-linejoin={ i.StrokeLineJoin }
		{ i.AttributesMap... }
	>
		<path d="M12 6v6l2 4"></path>
		<circle cx="12" cy="12" r="10"></circle>
	</svg>
}
